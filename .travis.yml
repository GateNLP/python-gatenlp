os: linux
dist: bionic
language: python
cache:
  pip: true
python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install -e .[all]

script:
  - python setup.py test

# for codecov.io
after_success:
  - <(curl -s https://codecov.io/bash)
